import { CustomDateFormat2Pipe } from './custom-date-format2.pipe';

describe('CustomDateFormat2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDateFormat2Pipe();
    expect(pipe).toBeTruthy();
  });
});
